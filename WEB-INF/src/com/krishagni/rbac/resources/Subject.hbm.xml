<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-mapping PUBLIC
    "-//Hibernate/Hibernate Mapping DTD 3.0//EN"
    "http://hibernate.sourceforge.net/hibernate-mapping-3.0.dtd">

<hibernate-mapping>
  <class name="com.krishagni.rbac.domain.Subject" table="CATISSUE_USER">

    <id name="id" column="IDENTIFIER">
      <generator class="native">
        <param name="sequence">CATISSUE_USER_SEQ</param>
      </generator>
    </id>
       
    <set 
      name="roles" 
      inverse="true" 
      cascade="all-delete-orphan"
      table="RBAC_SUBJECT_ROLES">
      <key column="SUBJECT_ID"/>
      <one-to-many class="com.krishagni.rbac.domain.SubjectRole"/>
    </set>
    
    <sql-query name="canUserAccess" >
      <return-scalar column="SUBJECT_ID" type="long"/>
      
      select 
        SUBJECT_ID
      from
        RBAC_SUBJECT_ACCESS_VIEW
      where
        SUBJECT_ID = :subjectId and
        RESOURCE = :resource and
        OPERATION = :operation and
        (CP_ID = :cpId or CP_ID is null) 
    </sql-query>
    
    <sql-query name="canUserAccessOnSite" >
      <return-scalar column="SUBJECT_ID" type="long"/>
      
      select 
        SUBJECT_ID
      from
        RBAC_SUBJECT_ACCESS_VIEW
      where
        SUBJECT_ID = :subjectId and
        RESOURCE = :resource and
        OPERATION = :operation and
        (CP_ID = :cpId or CP_ID is null) and
        (SITE_ID in (:siteIds) or SITE_ID is null)
    </sql-query>
    
    <sql-query name="getCpSiteAccessInfo">
      <return-scalar column="CP_ID" type="long"/>
      <return-scalar column="SITE_ID" type="long"/>

      select
        CP_ID, SITE_ID
      from 
        RBAC_SUBJECT_ACCESS_VIEW
      where
        RESOURCE = :resource 
        and OPERATION = :operation
        and SUBJECT_ID = :userId
    </sql-query>
  </class>  
</hibernate-mapping>
